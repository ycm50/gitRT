// Shared menu strings — English（《技术实现设计》§3.3）
// 由 src/gui/resources.rc 与 src/shell/shell_strings.rc 共同 #include。
// 注意：本文件必须保持**片段**形态（只有 KEY "value" 行）。

  IDS_GROUP_REPO      "Repository"
  IDS_GROUP_COMMIT    "Commit"
  IDS_GROUP_SYNC      "Sync"
  IDS_GROUP_BRANCH    "Branch & Tag"
  IDS_GROUP_INSPECT   "Inspect"
  IDS_GROUP_STASH     "Stash"
  IDS_GROUP_ADVANCED  "Advanced"
  IDS_GROUP_APP       "App"

  IDS_CMD_REPO_INIT       "Initialize repository here"
  IDS_CMD_REPO_CLONE      "Clone here..."
  IDS_CMD_APP_CONSOLE     "Open GitRT console"
  IDS_CMD_APP_TERMINAL    "Open terminal"
  IDS_CMD_COMMIT_STAGE    "Stage selection"
  IDS_CMD_COMMIT_UNSTAGE  "Unstage"
  IDS_CMD_COMMIT_DISCARD  "Discard selected changes"
  IDS_CMD_COMMIT_IGNORE   "Add to .gitignore"
  IDS_CMD_COMMIT_COMMIT   "Commit selection..."
  IDS_CMD_COMMIT_AMEND    "Amend last commit..."
  IDS_CMD_COMMIT_UNDO     "Undo last commit (keep changes)"
  IDS_CMD_SYNC_PULL       "Pull"
  IDS_CMD_SYNC_PUSH       "Push"
  IDS_CMD_SYNC_FETCH      "Fetch"
  IDS_CMD_SYNC_SYNC       "Sync (pull + push)"
  IDS_CMD_BRANCH_SWITCH   "Switch branch"
  IDS_CMD_BRANCH_NEW      "New branch"
  IDS_CMD_BRANCH_MERGE    "Merge..."
  IDS_CMD_BRANCH_REBASE   "Rebase onto..."
  IDS_CMD_BRANCH_DELETE   "Delete branch..."
  IDS_CMD_INSPECT_LOG     "Commit history"
  IDS_CMD_INSPECT_DIFF    "View diff"
  IDS_CMD_INSPECT_FILELOG "File history"
  IDS_CMD_INSPECT_STATUS  "Status panel"
  IDS_CMD_STASH_PUSH      "Stash changes..."
  IDS_CMD_STASH_POP       "Pop latest stash"
  IDS_CMD_ADV_CLEAN       "Clean untracked files..."
  IDS_CMD_ADV_RESET       "Reset to..."
  IDS_CMD_ADV_MAINTENANCE "Garbage collection (git gc)"
  IDS_CMD_APP_SETTINGS    "Preferences..."
  IDS_CMD_APP_DOCTOR      "Self-check"
  IDS_CMD_APP_AI          "AI assistant (natural language to command)"

  IDS_FLAG_AMEND             "Amend last commit (--amend)"
  IDS_FLAG_SIGNOFF           "Add sign-off line (--signoff)"
  IDS_FLAG_NO_VERIFY         "Skip hooks (--no-verify) [danger]"
  IDS_FLAG_INCLUDE_UNTRACKED "Include untracked files (--include-untracked)"
  IDS_FLAG_KEEP_INDEX        "Keep the index (--keep-index)"
  IDS_FLAG_INDEX             "Restore index state (--index)"
  IDS_FLAG_REBASE            "Use rebase (--rebase)"
  IDS_FLAG_FF_ONLY           "Fast-forward only (--ff-only)"
  IDS_FLAG_PRUNE             "Prune deleted remote branches (--prune)"
  IDS_FLAG_AUTOSTASH         "Auto-stash (--autostash)"
  IDS_FLAG_TAGS              "Include tags (--tags)"
  IDS_FLAG_SET_UPSTREAM      "Set upstream (--set-upstream)"
  IDS_FLAG_DRY_RUN           "Dry run, do not change anything (--dry-run)"
  IDS_FLAG_FORCE_WITH_LEASE  "Force push (safe --force-with-lease) [danger]"
  IDS_FLAG_ALL               "All remotes (--all)"
  IDS_FLAG_NO_FF             "No fast-forward (--no-ff)"
  IDS_FLAG_SQUASH            "Squash into one commit (--squash)"
  IDS_FLAG_INTERACTIVE       "Interactive rebase (--interactive) [danger]"
  IDS_FLAG_FORCE_DELETE      "Force delete unmerged branch (-D) [danger]"
  IDS_FLAG_INTENT_TO_ADD     "Intent to add only (--intent-to-add)"
  IDS_FLAG_RECURSE_SUBMODULE "Recurse submodules (--recurse-submodules)"
  IDS_FLAG_STAGED            "Compare the index (--staged)"
  IDS_FLAG_STAT              "Stat only (--stat)"
  IDS_FLAG_NAME_ONLY         "File names only (--name-only)"
  IDS_FLAG_CLEAN_DIRS        "Also remove directories (-d)"
  IDS_FLAG_CLEAN_IGNORED     "Include ignored files (-x) [danger]"
  IDS_FLAG_RESET_SOFT        "soft: keep changes staged"
  IDS_FLAG_RESET_MIXED       "mixed: keep changes in the work tree"
  IDS_FLAG_RESET_HARD        "hard: discard all changes [danger]"
  IDS_FLAG_AGGRESSIVE        "Aggressive optimization (--aggressive)"
  IDS_FLAG_BRANCH_VALUE      "Initial branch name"
  IDS_FLAG_TRACK_VALUE       "Track upstream (may be empty)"
IDS_FLAG_CLONE_DEPTH        "Depth --depth (shallow, e.g. 1; empty = full clone)"
IDS_FLAG_CLONE_SINGLE       "Single branch --single-branch"
IDS_FLAG_CLONE_BRANCH       "Branch --branch (empty = default branch)"
IDS_FLAG_CLONE_NO_TAGS      "No tags --no-tags"
IDS_FLAG_CLONE_FILTER       "Partial clone --filter (e.g. blob:none)"
  IDS_TERM_GIT_BASH          "Git Bash"
  IDS_TERM_POWERSHELL        "PowerShell"
  IDS_TERM_WT                "Windows Terminal"
  IDS_TERM_CMD               "Command Prompt"

  IDS_MENU_ROOT          "GitRT"
  IDS_MENU_MORE          "More..."
  IDS_MENU_NO_GIT        "git.exe not found - click to set the path"
  IDS_MENU_IN_PROGRESS   "! Repository operation unfinished (merge/rebase in progress)"
  IDS_MENU_STATE_UNKNOWN "State unknown (computing...)"

  IDS_LABEL_REPO         "Repository"

  IDS_TIP_ROOT           "GitRT - Git actions back in the File Explorer context menu"
  IDS_TIP_ROOT_APP       "Open the GitRT window (pick a Git action there)"
  IDS_TIP_FLAG           "Click to toggle and save this switch (runs no Git command)"
  IDS_TIP_GROUP          "Expand submenu"
  IDS_TIP_NEED_REPO      "Not a Git repository"
  IDS_TIP_NEED_SELECTION "Select a file or folder first"
  IDS_TIP_INTERNAL       "Handled internally by GitRT"

  IDS_MSG_REQ_MISSING    "The menu request is missing or expired"
  IDS_MSG_REQ_FORWARDED  "Request received from the context menu"
