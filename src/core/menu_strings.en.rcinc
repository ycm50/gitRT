// Shared menu strings — English（《技术实现设计》§3.3）
// 由 src/gui/resources.rc 与 src/shell/shell_strings.rc 共同 #include。
// 注意：本文件必须保持**片段**形态（只有 KEY "value" 行）。

  IDS_GROUP_REPO      "Repository"
  IDS_GROUP_COMMIT    "Commit"
  IDS_GROUP_SYNC      "Sync"
  IDS_GROUP_BRANCH    "Branch & Tag"
  IDS_GROUP_INSPECT   "Inspect"
  IDS_GROUP_STASH     "Stash"
  IDS_GROUP_ADVANCED  "Advanced"
  IDS_GROUP_APP       "App"

  IDS_CMD_REPO_INIT       "Initialize repository here"
  IDS_CMD_REPO_CLONE      "Clone here..."
  IDS_CMD_APP_CONSOLE     "Open GitRT console"
  IDS_CMD_APP_TERMINAL    "Open terminal"
  IDS_CMD_COMMIT_STAGE    "Stage selection"
  IDS_CMD_COMMIT_UNSTAGE  "Unstage"
  IDS_CMD_COMMIT_DISCARD  "Discard selected changes"
  IDS_CMD_COMMIT_IGNORE   "Add to .gitignore"
  IDS_CMD_COMMIT_COMMIT   "Commit selection..."
  IDS_CMD_COMMIT_AMEND    "Amend last commit..."
  IDS_CMD_COMMIT_UNDO     "Undo last commit (keep changes)"
  IDS_CMD_SYNC_PULL       "Pull"
  IDS_CMD_SYNC_PUSH       "Push"
  IDS_CMD_SYNC_FETCH      "Fetch"
  IDS_CMD_SYNC_SYNC       "Sync (pull + push)"
  IDS_CMD_BRANCH_SWITCH   "Switch branch"
  IDS_CMD_BRANCH_NEW      "New branch"
  IDS_CMD_BRANCH_MERGE    "Merge..."
  IDS_CMD_BRANCH_REBASE   "Rebase onto..."
  IDS_CMD_BRANCH_DELETE   "Delete branch..."
  // ---- tags & releases (tag.* / release.*)
  IDS_CMD_TAG_LIST        "Tag list"
  IDS_CMD_TAG_CREATE      "Create tag..."
  IDS_CMD_TAG_PUSH        "Push tag..."
  IDS_CMD_TAG_DELETE      "Delete tag..."
  IDS_CMD_RELEASE_LIST    "Release list"
  IDS_CMD_RELEASE_CREATE  "Create release..."
  IDS_CMD_INSPECT_LOG     "Commit history"
  IDS_CMD_INSPECT_DIFF    "View diff"
  IDS_CMD_INSPECT_FILELOG "File history"
  IDS_CMD_INSPECT_STATUS  "Status panel"
  IDS_CMD_STASH_PUSH      "Stash changes..."
  IDS_CMD_STASH_POP       "Pop latest stash"
  IDS_CMD_ADV_CLEAN       "Clean untracked files..."
  IDS_CMD_ADV_RESET       "Reset to..."
  IDS_CMD_ADV_MAINTENANCE "Garbage collection (git gc)"
  IDS_CMD_APP_SETTINGS    "Preferences..."
  IDS_CMD_APP_DOCTOR      "Self-check"
  IDS_CMD_APP_AI          "AI assistant (natural language to command)"

  IDS_FLAG_AMEND             "Amend last commit (--amend)"
  IDS_FLAG_SIGNOFF           "Add sign-off line (--signoff)"
  IDS_FLAG_NO_VERIFY         "Skip hooks (--no-verify) [danger]"
  IDS_FLAG_INCLUDE_UNTRACKED "Include untracked files (--include-untracked)"
  IDS_FLAG_KEEP_INDEX        "Keep the index (--keep-index)"
  IDS_FLAG_INDEX             "Restore index state (--index)"
  IDS_FLAG_REBASE            "Use rebase (--rebase)"
  IDS_FLAG_FF_ONLY           "Fast-forward only (--ff-only)"
  IDS_FLAG_PRUNE             "Prune deleted remote branches (--prune)"
  IDS_FLAG_AUTOSTASH         "Auto-stash (--autostash)"
  IDS_FLAG_TAGS              "Include tags (--tags)"
  IDS_FLAG_SET_UPSTREAM      "Set upstream (--set-upstream)"
  IDS_FLAG_DRY_RUN           "Dry run, do not change anything (--dry-run)"
  IDS_FLAG_FORCE_WITH_LEASE  "Force push (safe --force-with-lease) [danger]"
  IDS_FLAG_ALL               "All remotes (--all)"
  IDS_FLAG_NO_FF             "No fast-forward (--no-ff)"
  IDS_FLAG_SQUASH            "Squash into one commit (--squash)"
  IDS_FLAG_INTERACTIVE       "Interactive rebase (--interactive) [danger]"
  IDS_FLAG_FORCE_DELETE      "Force delete unmerged branch (-D) [danger]"
  IDS_FLAG_INTENT_TO_ADD     "Intent to add only (--intent-to-add)"
  IDS_FLAG_RECURSE_SUBMODULE "Recurse submodules (--recurse-submodules)"
  IDS_FLAG_STAGED            "Compare the index (--staged)"
  IDS_FLAG_STAT              "Stat only (--stat)"
  IDS_FLAG_NAME_ONLY         "File names only (--name-only)"
  IDS_FLAG_CLEAN_DIRS        "Also remove directories (-d)"
  IDS_FLAG_CLEAN_IGNORED     "Include ignored files (-x) [danger]"
  IDS_FLAG_RESET_SOFT        "soft: keep changes staged"
  IDS_FLAG_RESET_MIXED       "mixed: keep changes in the work tree"
  IDS_FLAG_RESET_HARD        "hard: discard all changes [danger]"
  IDS_FLAG_AGGRESSIVE        "Aggressive optimization (--aggressive)"
  IDS_FLAG_BRANCH_VALUE      "Initial branch name"
  IDS_FLAG_TRACK_VALUE       "Track upstream (may be empty)"
IDS_FLAG_CLONE_DEPTH        "Depth --depth (shallow, e.g. 1; empty = full clone)"
IDS_FLAG_CLONE_SINGLE       "Single branch --single-branch"
IDS_FLAG_CLONE_BRANCH       "Branch --branch (empty = default branch)"
IDS_FLAG_CLONE_NO_TAGS      "No tags --no-tags"
IDS_FLAG_CLONE_FILTER       "Partial clone --filter (e.g. blob:none)"
  IDS_TERM_GIT_BASH          "Git Bash"
  IDS_TERM_POWERSHELL        "PowerShell"
  IDS_TERM_WT                "Windows Terminal"
  IDS_TERM_CMD               "Command Prompt"

  // ---- tag / release options (tag.* / release.*)
  IDS_FLAG_TAG_ANNOTATED     "Annotated tag (-a -m: records tagger, date and message)"
  IDS_FLAG_TAG_FORCE         "Overwrite an existing tag (-f) [danger]"
  IDS_FLAG_TAG_TARGET        "Target revision --target (empty = HEAD; accepts a hash or branch)"
  IDS_FLAG_PUSH_ALL_TAGS     "Push **all** local tags (--tags)"
  IDS_FLAG_REMOTE_VALUE      "Remote name (empty = origin)"
  IDS_FLAG_DELETE_REMOTE     "Also delete the same tag on the remote"
  IDS_FLAG_RELEASE_TITLE     "Release title --title (empty = tag name)"
  IDS_FLAG_RELEASE_NOTES     "Release notes --notes (empty and no generate = blank body)"
  IDS_FLAG_GENERATE_NOTES    "Generate notes automatically (--generate-notes)"
  IDS_FLAG_DRAFT             "Save as draft (--draft: only you can see it)"
  IDS_FLAG_PRERELEASE        "Mark as pre-release (--prerelease)"

  IDS_MENU_ROOT          "GitRT"
  IDS_MENU_MORE          "More..."
  IDS_MENU_NO_GIT        "git.exe not found - click to set the path"
  IDS_MENU_IN_PROGRESS   "! Repository operation unfinished (merge/rebase in progress)"
  IDS_MENU_STATE_UNKNOWN "State unknown (computing...)"

  IDS_LABEL_REPO         "Repository"

  IDS_TIP_ROOT           "GitRT - Git actions back in the File Explorer context menu"
  IDS_TIP_ROOT_APP       "Open the GitRT window (pick a Git action there)"
  IDS_TIP_FLAG           "Click to toggle and save this switch (runs no Git command)"
  IDS_TIP_GROUP          "Expand submenu"
  IDS_TIP_NEED_REPO      "Not a Git repository"
  IDS_TIP_NEED_SELECTION "Select a file or folder first"
  IDS_TIP_INTERNAL       "Handled internally by GitRT"

  // ---- tags / releases
  IDS_TAG_PANEL_HINT     "A tag is a fixed name for one commit (like v1.0.0); creating a Release needs the GitHub CLI (gh)"
  IDS_TAG_EMPTY          "(No tags yet: use Create tag...)"
  IDS_TAG_NEED_NAME      "Enter a tag name (e.g. v1.0.0)"
  IDS_TAG_ANNOTATED_HINT "An annotated tag stores tagger, date and message; a lightweight tag is just a name"
  IDS_TAG_FORCE_HINT     "If the tag already exists you must force (-f): that moves the name to another commit"
  IDS_TAG_PUSH_ALL_HINT  "Pushes **all** local tags, not just the selected one"
  IDS_TAG_DELETE_WARN    "Deleting a tag removes only the name, never a commit; the remote tag must be deleted separately"
  IDS_RELEASE_NEED_GH    "GitHub CLI (gh) not found - releases need it: winget install GitHub.cli"
  IDS_RELEASE_NEED_TAG   "The tag must exist locally before a release can be created for it"
  IDS_RELEASE_DRAFT_HINT "A draft is visible only to you until you publish it"
  IDS_RELEASE_PUBLIC_HINT "Once published it is **public** to anyone who can read the repository"

  // ---- Tag window (phase 2: dedicated GUI window, src/gui/tag_window.cpp)
  IDS_TITLE_TAG          "Tags"
  IDS_TG_LBL_LIST        "Tags (select one to push or delete; Type shows annotated/lightweight)"
  IDS_TG_COL_NAME        "Name"
  IDS_TG_COL_HASH        "Commit"
  IDS_TG_COL_TYPE        "Type"
  IDS_TG_COL_DATE        "Date"
  IDS_TG_COL_SUBJECT     "Subject"
  IDS_TG_COL_REMOTE      "Remote"
  IDS_TG_TYPE_ANNOTATED  "annotated"
  IDS_TG_TYPE_LIGHT      "lightweight"
  IDS_TG_REMOTE_YES      "yes"
  IDS_TG_REMOTE_NO       "no"
  IDS_TG_LBL_CREATE      "Create tag"
  IDS_TG_LBL_NAME        "Tag name (for example v1.0.0)"
  IDS_TG_LBL_TARGET      "Target revision --target (empty = HEAD; accepts a hash or branch)"
  IDS_TG_LBL_MESSAGE     "Tag message (only an annotated tag stores it)"
  IDS_TG_ANNOTATED       "Annotated tag (-a -m)"
  IDS_TG_FORCE           "Overwrite an existing tag (-f)"
  IDS_TG_LBL_PUSH        "Push tag"
  IDS_TG_PUSH_ALL        "Push all tags (--tags)"
  IDS_TG_LBL_DELETE      "Delete tag"
  IDS_TG_DEL_LOCAL       "Delete the local tag"
  IDS_TG_LBL_REMOTE      "Remote name (empty = origin)"
  IDS_TG_DEL_REMOTE      "Also delete the same tag on the remote"
  IDS_TG_BTN_CREATE      "Create tag"
  IDS_TG_BTN_PUSH        "Push"
  IDS_TG_BTN_DELETE      "Delete"
  IDS_TG_SEL_NONE        "Select a tag, or type a name"
  IDS_TG_SEL_OK          "Selected: {name} ({hash}) {kind}"
  IDS_TG_RUNNING         "Running..."
  IDS_TG_DONE            "Done"
  IDS_TG_FAILED          "Failed: {msg}"
  IDS_TG_NO_TAGS         "(No tags yet: create one below)"
  IDS_TG_LBL_LOG         "Run log"
  IDS_TG_CONFIRM_TITLE   "Confirm"
  IDS_TG_CONFIRM_BODY    "Will run:\n{cmds}\n\n{warns}Continue?"
  IDS_TG_DEL_CONFIRM_TITLE "Delete tag (cannot be undone)"
  IDS_TG_DEL_CONFIRM_BODY "Will delete tag {name}:\n{cmds}\n\nDeleting a tag removes only the name, never a commit. Continue?"
  IDS_TG_NEED_LOCAL      "Remote-only deletion is not supported: the delete plan always removes the local tag first, so check Delete the local tag"
  IDS_TG_NEED_ONE        "Check at least one: delete the local tag / also delete the tag on the remote"
  IDS_TG_CREATED_OK      "Done: tag {name} created"
  IDS_TG_PLAN_OK         "Command ready: the matching button asks for confirmation again"

  // ---- Release window (phase 2: dedicated GUI window, src/gui/release_window.cpp)
  IDS_TITLE_RELEASE      "Releases (GitHub)"
  IDS_REL_LBL_LIST       "Release list (read through the GitHub CLI)"
  IDS_REL_COL_TAG        "Tag"
  IDS_REL_COL_NAME       "Title"
  IDS_REL_COL_DATE       "Published"
  IDS_REL_COL_FLAGS      "State"
  IDS_REL_DRAFT_TAG      "draft"
  IDS_REL_PRE_TAG        "pre-release"
  IDS_REL_LBL_CREATE     "Create release"
  IDS_REL_LBL_TAG        "Tag (pick an existing tag or type one)"
  IDS_REL_LBL_TITLE      "Title (empty = the tag name)"
  IDS_REL_LBL_NOTES      "Release notes (multi-line; empty and no generate = blank body)"
  IDS_REL_GENERATE       "Generate notes automatically (--generate-notes)"
  IDS_REL_DRAFT_CHK      "Save as draft (--draft)"
  IDS_REL_PRE_CHK        "Mark as pre-release (--prerelease)"
  IDS_REL_LBL_ASSETS     "Assets (any number)"
  IDS_REL_BTN_PICK       "Choose files..."
  IDS_REL_BTN_REMOVE     "Remove selected"
  IDS_REL_LBL_LOG        "Run log"
  IDS_REL_BTN_CREATE     "Create release"
  IDS_REL_SEL_NONE       "Fill in the tag to create a release"
  IDS_REL_SEL_OK         "Will create a release for {tag}"
  IDS_REL_RUNNING        "Running..."
  IDS_REL_DONE           "Done"
  IDS_REL_FAILED         "Failed: {msg}"
  IDS_REL_NO_RELEASES    "(No releases yet)"
  IDS_REL_CONFIRM_TITLE  "Confirm release creation"
  IDS_REL_CONFIRM_BODY   "Will run:\n{cmds}\n\n{warns}Once published it is public to anyone who can read the repository. Continue?"

  IDS_MSG_REQ_MISSING    "The menu request is missing or expired"
  IDS_MSG_REQ_FORWARDED  "Request received from the context menu"
